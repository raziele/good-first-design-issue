name: Preview Deploy (Railway)

on:
  pull_request:
    types: [opened, synchronize, closed]

jobs:
  deploy-preview:
    if: startsWith(github.head_ref, 'agent/feature-') && github.event.action != 'closed'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: "20"

      - name: Install Railway CLI
        run: npm install -g @railway/cli

      - name: Deploy backend to preview
        env:
          RAILWAY_TOKEN: ${{ secrets.RAILWAY_TOKEN }}
        run: railway up --service backend --environment pr-${{ github.event.number }}

      - name: Deploy frontend to preview
        env:
          RAILWAY_TOKEN: ${{ secrets.RAILWAY_TOKEN }}
        run: railway up --service frontend --environment pr-${{ github.event.number }}

      - name: Get preview URLs and comment on PR
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          RAILWAY_TOKEN: ${{ secrets.RAILWAY_TOKEN }}
        run: |
          BACKEND_URL=$(railway status --service backend --environment pr-${{ github.event.number }} --json | jq -r '.url // "pending"')
          FRONTEND_URL=$(railway status --service frontend --environment pr-${{ github.event.number }} --json | jq -r '.url // "pending"')
          gh pr comment ${{ github.event.number }} --body "## Sandbox Preview

          The preview environment is ready.

          | Service | URL |
          |---------|-----|
          | Frontend | $FRONTEND_URL |
          | Backend API | $BACKEND_URL |

          _Deployed via Railway — environment \`pr-${{ github.event.number }}\`_"

  teardown-preview:
    if: startsWith(github.head_ref, 'agent/feature-') && github.event.action == 'closed'
    runs-on: ubuntu-latest
    steps:
      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: "20"

      - name: Install Railway CLI
        run: npm install -g @railway/cli

      - name: Tear down preview environment
        env:
          RAILWAY_TOKEN: ${{ secrets.RAILWAY_TOKEN }}
        run: railway environment delete pr-${{ github.event.number }} --yes
        continue-on-error: true
