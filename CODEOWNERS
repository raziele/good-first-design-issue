# Specs require human approval — never auto-merged
specs/ @human-reviewers

# Tests are auto-committed by Agent 1 directly to main (no PR gate)
# tests/ — no CODEOWNERS restriction

# Agent configs require human approval
agents/ @human-reviewers

# Pipeline config requires human approval
pipeline/ @human-reviewers

# Source code, docs are in code PRs — human reviews after sandbox
# src/ — reviewed via PR after Railway preview
# README.md, CLAUDE.md — updated by Agent 4, included in same PR
